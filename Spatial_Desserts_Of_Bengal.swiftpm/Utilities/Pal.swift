import SwiftUI
import UIKit

struct Pal {
    static let cRedTop = Color(hex: "FF6B6B")
    static let cRedBot = Color(hex: "C92A2A")
    static let cRedShd = Color(hex: "5C1B1B")
    static let cYelTop = Color(hex: "FFD43B")
    static let cYelBot = Color(hex: "F08C00")
    static let cBrnTxt = Color(hex: "3E2723")
    static let cTabTop = Color(hex: "FFF9DB")
    static let cTabBot = Color(hex: "8D6E63")
    static let cGold = Color(hex: "FFD700")
    static let cCyan = Color(hex: "00FFFF")
    static let cGreen = Color(hex: "00FF00")
    static let cRed = Color(hex: "FF0000")
    static let cWhite = Color.white
    
    static let cSysBg = UIColor(red: 0.05, green: 0.08, blue: 0.15, alpha: 0.85)
    static let cSysBorder = UIColor(red: 0.0, green: 0.66, blue: 1.0, alpha: 1.0)
    static let cSysText = UIColor.white
    
    
    static let cTextOnBright = Color.black.opacity(0.8)
}

